# SiteLens web — copy to .env.local and fill from `supabase status`.
# These are the local-stack values; cloud differs only in these.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
